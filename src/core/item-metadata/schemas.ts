import{z}from'zod';import{ITEM_METADATA_KINDS,METADATA_ITEMTYPES}from'./types.js';const base=z.object({kind:z.enum(ITEM_METADATA_KINDS),itemtype:z.enum(METADATA_ITEMTYPES),item_id:z.number().int().min(1)});export const metadataListSchema=base.strict();export const metadataGetSchema=base.extend({id:z.number().int().min(1).optional()}).strict().superRefine((v,c)=>{if(v.kind==='note'&&!v.id)c.addIssue({code:'custom',path:['id'],message:'note requires id'})});const fields=z.record(z.string(),z.unknown()).refine(v=>Object.keys(v).length>0);export const metadataCreateSchema=base.extend({fields,correlation_id:z.string().uuid()}).strict();export const metadataUpdateSchema=base.extend({id:z.number().int().min(1).optional(),fields,correlation_id:z.string().uuid()}).strict().superRefine((v,c)=>{if(v.kind==='note'&&!v.id)c.addIssue({code:'custom',path:['id'],message:'note requires id'})});export const metadataDeletePreviewSchema=metadataGetSchema;export const metadataDeleteSchema=base.extend({id:z.number().int().min(1).optional(),preview_fingerprint:z.string().regex(/^[a-f0-9]{64}$/),confirmation:z.literal('I_HAVE_VERIFIED_THE_ITEM_METADATA_DELETE'),correlation_id:z.string().uuid()}).strict().superRefine((v,c)=>{if(v.kind==='note'&&!v.id)c.addIssue({code:'custom',path:['id'],message:'note requires id'})});
