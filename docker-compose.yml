services:
  glpi-mcp:
    build:
      context: .
      args:
        MCP_GLPI_VERSION: ${MCP_GLPI_VERSION:-v3.3.0}
        SUPERGATEWAY_VERSION: ${SUPERGATEWAY_VERSION:-3.4.3}
    image: doosys/glpi-mcp-docker:${MCP_GLPI_VERSION:-v3.3.0}
    container_name: glpi-mcp
    restart: unless-stopped
    environment:
      GLPI_URL: ${GLPI_URL}
      GLPI_APP_TOKEN: ${GLPI_APP_TOKEN}
      GLPI_USER_TOKEN: ${GLPI_USER_TOKEN}
      GLPI_USERNAME: ${GLPI_USERNAME:-}
      GLPI_PASSWORD: ${GLPI_PASSWORD:-}
      GLPI_TIMEOUT_MS: ${GLPI_TIMEOUT_MS:-15000}
      GLPI_MAX_RETRIES: ${GLPI_MAX_RETRIES:-2}
      GLPI_DEBUG: ${GLPI_DEBUG:-}
      MCP_GLPI_VERSION: ${MCP_GLPI_VERSION:-v3.3.0}
      MCP_PORT: ${MCP_PORT:-8000}
      MCP_PATH: ${MCP_PATH:-/mcp}
      MCP_HEALTH_PATH: ${MCP_HEALTH_PATH:-/healthz}
      MCP_SESSION_TIMEOUT_MS: ${MCP_SESSION_TIMEOUT_MS:-600000}
      MCP_LOG_LEVEL: ${MCP_LOG_LEVEL:-info}
    ports:
      - "${MCP_BIND_ADDRESS:-0.0.0.0}:${MCP_PORT:-8000}:${MCP_PORT:-8000}"
    networks:
      - glpi-mcp
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - \"http://127.0.0.1:$${MCP_PORT}$${MCP_HEALTH_PATH}\" | grep -q ok"]
      interval: 30s
      timeout: 5s
      start_period: 20s
      retries: 3

networks:
  glpi-mcp:
    name: glpi-mcp
    driver: bridge
